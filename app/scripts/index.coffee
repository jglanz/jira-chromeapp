'use strict';

document.addEventListener 'DOMContentLoaded', ->
	body = document.getElementsByTagName('body')
	# if body.length > 0
	# 	frame = document.createElement('webview')
	# 	frame.src = 'https://getrads.atlassian.net'
	#
	# 	body[0].appendChild(frame)

, false
